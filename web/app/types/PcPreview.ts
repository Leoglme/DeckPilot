/** A cylindrical segment around the AIO pump cap. */
export type PcPreviewPumpSegment = {
  angle: string
  luminance: string
}

/** RAM stick horizontal offset in the 3D volume. */
export type PcPreviewRamStick = {
  offsetX: string
}
